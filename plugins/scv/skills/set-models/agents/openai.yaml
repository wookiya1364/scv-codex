interface:
  display_name: "SCV Model Policy"
  short_description: "Inspect Codex model policy compatibility for SCV"
  default_prompt: "Use $scv:set-models to inspect or configure the Codex model policy used with SCV."

policy:
  allow_implicit_invocation: true
