interface:
  display_name: "SCV Promote"
  short_description: "Turn raw project material into a plan and tests"
  default_prompt: "Use $scv:promote to refine raw material into an executable plan and tests."

policy:
  allow_implicit_invocation: false
