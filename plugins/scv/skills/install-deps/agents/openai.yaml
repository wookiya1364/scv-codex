interface:
  display_name: "SCV Install Dependencies"
  short_description: "Detect missing SCV command-line dependencies"
  default_prompt: "Use $scv:install-deps to diagnose the command-line tools SCV needs."

policy:
  allow_implicit_invocation: false
