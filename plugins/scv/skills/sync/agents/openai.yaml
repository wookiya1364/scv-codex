interface:
  display_name: "SCV Sync"
  short_description: "Sync SCV templates and detect plan-to-code drift"
  default_prompt: "Use $scv:sync to preview template updates and check active plans for drift."

policy:
  allow_implicit_invocation: false
