# 0.29.0-codex.1

Core 0.29.0 — effort governor 를 싣습니다.

## 작업 무게에 맞춰 알아서 돕니다

`$scv:work` / `$scv:codegen` 이 구현 전에 계획의 실행 밴드를 판정합니다
(백테스트 13/14 의 3규칙). 세션 추론강도 설정은 건드리지 않습니다 — 조절되는
것은 실행 방식입니다. standard 계획은 단일 패스 검증만 수행하고, 이 호스트에는
서브에이전트 팬아웃이 없으므로 orchestration 밴드는 순차 다중 렌즈로 강하합니다
(계약 동일). `.env` 의 `SCV_EFFORT_MODE=auto|ask|off`, off 는 이전 동작과 완전
동일. 계획 frontmatter 의 `effort_class:` 선언이 항상 이깁니다.

## TESTS 내구성 규칙

PROMOTE.md 체크리스트의 "How-to-run 은 아카이브 후에도 참" 규칙과 tests-smell
경고 2종이 함께 들어옵니다.
