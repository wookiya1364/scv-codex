# 0.45.0-codex.1

Core 0.45.0 동기화.

## 설정 등록부에 모델 정책 키가 생겼습니다

Claude Code 래퍼 쪽 변경(명령이 세션 모델을 바꾸지 않게)에 맞춰 코어 설정 등록부에
`SCV_MODEL_POLICY`(기본 session-default) 가 추가됐습니다. Codex 래퍼는 원래 모델 지정이
없어 동작 변화가 없습니다 — 설정 파일에 키 하나가 더 보일 뿐입니다.

- Core pin `0.44.0` → `0.45.0`.
