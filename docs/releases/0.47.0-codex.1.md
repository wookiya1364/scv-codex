# 0.47.0-codex.1

Core 0.47.0 동기화. Codex 래퍼 자체의 동작 변경은 없습니다.

## 코어에 들어온 것

- **비운 뒤 되찾기 템플릿** `on-session-start.sh` 와 스위치 `SCV_RESUME_RECAP`(기본 on). 대화를
  지우거나 압축·재개한 직후 진행 중 계획·최근 결정·활성 대화를 다시 싣는 훅입니다. Codex 래퍼는
  이 이벤트에 등가 훅을 **등록하지 않습니다** — 등가 이벤트가 확인되지 않았습니다. 템플릿은
  벤더링만 되고 아무 일도 하지 않습니다.
- **스킬 설명 길이 검사** `test-skill-descriptions.sh` — Claude Code 래퍼 전용(옆 체크아웃이 있을
  때만 돎). 이 저장소 CI 에서는 건너뜁니다.
- 프로버넌스 게이트가 promote/ → archive/ 로 옮겨진 계획도 "추가" 로 봅니다(`--no-renames`).

- Core pin `0.46.0` → `0.47.0`.
